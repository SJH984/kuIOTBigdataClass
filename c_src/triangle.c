#include <stdio.h>

int main(void)
{
	int base;
	int height;
	int area;

	scanf("%d" "%d", &base, &height);
	
	area = base * height / 2;
	
	printf("base: %d, height:   %d ---> area : %d", base, height, area);


	return 0;
	
}
